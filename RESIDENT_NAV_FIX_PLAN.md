# 입주민 앱 네비게이션 매칭 수정 계획

## 🚨 문제점 진단 (Diagnosis)
사용자 지적 사항: "카드 위젯과 페이지가 매칭 안되는 게 있다."

1.  **인피니트 가든(Garden) ↔ IoT 등록 불가**:
    *   현재 홈 화면의 '인피니트 가든'(`garden`) 타일이 `/iot-registration` (IoT 기기 등록)으로 연결되어 있습니다.
    *   **수정 방향**: 아직 'Smart Farm' 기능은 구현되지 않았으므로, 준비 중인 페이지(`ResidentPlaceholderPage`)로 연결하거나, `GardenPage` 껍데기를 만들어 연결해야 합니다. 여기서는 명확성을 위해 **'스마트 팜 (준비 중)'** 페이지로 연결합니다.

2.  **세탁/홈케어 ↔ 도어스텝 탭 매칭**:
    *   '세탁 서비스'(`laundry`) 타일과 '홈 케어'(`homecare`) 타일이 모두 `/doorstep`으로 연결되지만, 기본적으로 **첫 번째 탭(스마트 택배)**이 열립니다.
    *   **수정 방향**: `/doorstep` 라우트에 `?tab=` 쿼리 파라미터를 추가하여, 진입 시 해당 탭(세탁: tab 1, 홈케어: tab 2)이 바로 열리도록 수정합니다.

## 🛠️ 작업 계획 (Action Plan)

### 1단계: DoorstepManagerPage 탭 컨트롤 기능 추가
*   **파일**: `/lib/features/resident/ui/pages/doorstep_manager_page.dart`
*   **내용**: 생성자에서 `initialIndex`를 받아 `DefaultTabController`의 `initialIndex`로 설정하도록 수정합니다.

### 2단계: 라우터 수정 (Query Parameter 지원)
*   **파일**: `/lib/core/router/resident_router.dart`
*   **내용**: `/doorstep` 라우트에서 `state.queryParams['tab']`을 읽어 `DoorstepManagerPage`에 전달합니다.

### 3단계: 홈 화면 네비게이션 경로 수정
*   **파일**: `/lib/features/resident/ui/pages/resident_home_page.dart`
*   **내용**:
    *   `laundry` -> `/doorstep?tab=1`
    *   `homecare` -> `/doorstep?tab=2`
    *   `garden` -> `/garden` (새로운 라우트 추가 및 Placeholder 연결)

### 4단계: Garden 페이지 (Placeholder) 추가
*   **파일**: `/lib/core/router/resident_router.dart`
*   **내용**: `/garden` 라우트를 추가하고 `ResidentPlaceholderPage(title: '인피니트 가든')`로 연결합니다.

## 📅 진행 순서
1.  `DoorstepManagerPage` 수정 (탭 인덱스 지원)
2.  `ResidentRouter` 수정 (쿼리 파라미터 및 Garden 라우트 추가)
3.  `ResidentHomePage` 수정 (올바른 경로로 연결)
